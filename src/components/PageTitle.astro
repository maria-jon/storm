---
const { title, color } = Astro.props;
---

<h1 class={color}>
    {title}
</h1>

<style>
    .text-peachy-pink {
        color: var(--peachy-pink);
    }

    .text-wine-red {
        color: var(--wine-red);
    }
</style>